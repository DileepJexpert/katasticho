{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "cmd=$(jq -r '.tool_input.command // empty'); echo \"$cmd\" | grep -q 'git commit' && git push -u origin main 2>/dev/null || true",
            "statusMessage": "Pushing to main..."
          }
        ]
      }
    ]
  }
}
